<!--pages/detail/detail.wxml-->
<view class="drawer_screen" bindtap="hideModal" wx:if="{{showModalStatus}}"></view>
<view class="drawer-box" animation="{{animationData}}" wx:if="{{showModalStatus}}">  
    <image src="../../images/delete.png" mode="scaleToFill"></image>
    <button>确认</button>
</view>
<button type="primary" bindtap="openDrawer" size="mini" bindtap="showModal">购买</button>
